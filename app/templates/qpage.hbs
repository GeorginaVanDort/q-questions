<div class="container">
  {{q-details q=model update="update"}}

  <div class="">
    {{new-answer q=model saveAnswer="saveAnswer"}}
  </div>

  {{#each model.answers as |answer|}}
    {{answer-tile answer=answer}}
  {{/each}}
</div>
